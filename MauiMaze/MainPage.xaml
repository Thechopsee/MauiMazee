<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiMaze.MainPage"
             Title="">

    <ScrollView>
        <VerticalStackLayout
            Spacing="25"
            Padding="30,0"
            VerticalOptions="Center">

            <Image
                Source="dotnet_bot.png"
                SemanticProperties.Description="Cute dot net bot waving hi to you!"
                HeightRequest="200"
                HorizontalOptions="Center" />

            <Label
                Text="Hello, World!"
                SemanticProperties.HeadingLevel="Level1"
                FontSize="32"
                HorizontalOptions="Center" />

            <Label
                Text="Welcome to .NET Multi-platform App UI"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I"
                FontSize="18"
                HorizontalOptions="CenterAndExpand" />

            <StackLayout
                Orientation="Horizontal"
                HorizontalOptions="FillAndExpand"
                Margin="0,20,0,0">

                <Label
                Text=" "
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I"
                FontSize="18"
                HorizontalOptions="CenterAndExpand" />
                <VerticalStackLayout
                Spacing="25"
                Padding="30,0"
                VerticalOptions="Center"
                HorizontalOptions="CenterAndExpand">

                    <Entry
                    x:Name="NameEntry"
                    Placeholder="Enter your name"
                    HorizontalOptions="FillAndExpand" />

                    <!-- Příklad vstupního pole pro zadání emailu -->
                    <Entry
                    x:Name="EmailEntry"
                    Placeholder="Enter your email"
                    HorizontalOptions="FillAndExpand" />

                    <!-- Příklad tlačítka pro odeslání formuláře -->
                    <Button
                    x:Name="SubmitButton"
                    Text="Submit"

                    HorizontalOptions="Center" />
                </VerticalStackLayout>

                <Label
                Text=" "
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I"
                FontSize="18"
                HorizontalOptions="CenterAndExpand" />

            </StackLayout>
            

            <Button
                x:Name="Mazes"
                Text="Go to mazes"
                SemanticProperties.Hint="Maze menu"
                Clicked="mazeMenuNavigate"
                HorizontalOptions="Center" />
            <StackLayout
                Orientation="Horizontal"
                HorizontalOptions="FillAndExpand"
                Margin="0,20,0,0">

                <Button
                    x:Name="About"
                    Text="Button 1"
                    
                    HorizontalOptions="CenterAndExpand" />

                <Label
                    x:Name="Button2"
                    Text="Button 2"
                    
                    HorizontalOptions="CenterAndExpand" />

                <Button
                    x:Name="Settings"
                    Text="Button 3"
                    
                    HorizontalOptions="CenterAndExpand" />

            </StackLayout>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
